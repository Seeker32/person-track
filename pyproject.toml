[project]
name = "person-track"
version = "0.1.0"
description = "A person track system"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "supervision>=0.28.0",
    "trackers>=2.4.0",
    "ultralytics>=8.3.0",
    "pyyaml>=6.0",
]
